general {
  disable_loading_bar = true
  hide_cursor = true
}

background {
  monitor =
  path = screenshot
  blur_passes = 3
  blur_size = 7
}

label {
  monitor =
  text = cmd[date +"%H:%M:%S"]
  font_size = 72
  position = 0, 120
  halign = center
  valign = center
}

label {
  monitor =
  text = cmd[date +"%Y.%m.%d"]
  font_size = 22
  position = 0, 60
  halign = center
  valign = center
}

input-field {
  monitor =
  size = 320, 56
  position = 0, -80
  halign = center
  valign = center

  outline_thickness = 2
  dots_center = true
  fade_on_empty = false
  placeholder_text = Password
}
