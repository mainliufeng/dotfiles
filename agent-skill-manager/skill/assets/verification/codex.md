# Verification: codex

After any Codex install or update:

1. Confirm the expected directory exists in `~/.codex/skills`
2. If symlink-based, confirm it points to the intended source
3. For special installs, confirm representative subskills or generated outputs exist
4. Report any stale backups or duplicate installs if found
