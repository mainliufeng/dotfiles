# Verification: claude-code

After any Claude Code install or update:

1. Confirm the expected directory exists in `~/.claude/skills`
2. If symlink-based, confirm it points to the intended source
3. For special installs, confirm expected generated/runtime-visible skills exist
4. Report drift if Claude and Codex now intentionally differ
