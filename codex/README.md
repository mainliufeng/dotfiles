# Codex CLI Configuration

Managed configuration for `~/.codex`. Run `~/dotfiles/codex/link.sh` (or the top-level `link.sh`) to symlink `config.toml` into the expected location.

The configuration enables the Chrome DevTools MCP server and points it to a manually launched Chrome instance.
